let
    bool a = true;
    bool b = false;
in
    if(a && b)
        then {1}
        else {
            if(b)
                then {2}
                else {
                   if(a || b)
                        then {3}
                        else {4}
                    }
            };
