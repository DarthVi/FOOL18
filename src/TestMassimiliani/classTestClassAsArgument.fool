class pippo(int a)
{
    int retVal()
        a + 1;

    bool gee()
        true;
}

class pluto extends pippo(int a; int b)
{
    int retVal()
        a + 2;

    bool foo(int c)
        if b > 0 then { true } else { false };
}

let
    int funArgClass(pippo x)
        x.retVal();

in
    print(funArgClass(new pluto(4, -1)));
    print(funArgClass(new pippo(3)));