class pippo(int a)
{
    int retVal()
        a + 1;

    bool gee()
        true;
}

class pluto extends pippo(int a; int b)
{
    int retVal()
        a + 2;

    bool foo(int c)
        if b > 0 then { true } else { false };
}

class paperino(int c)
{
    int bla(pippo a)
        a.retVal();

    int retC()
        c;

    int hoo(paperino p)
        p.retC();
}

let
   paperino pap = new paperino(5);

in
    print(pap.hoo(new paperino(8)));
    print(pap.bla(new pippo(10)));