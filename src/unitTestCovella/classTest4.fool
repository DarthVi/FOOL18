class Pippo (bool postazione; int portatili)
{
    int f(int x, int y)
        x + portatili;
}

class Pluto extends Pippo (bool postazione; int portatili; int quaderno)
{
    void g(bool val)
        let
            bool x;
        in
            val = x;
}

class Paperino extends Pluto(bool postazione; int portatili; int quaderno; int qui)
{
    bool h()
        if(postazione) then { quaderno } else { qui };
}

let
    int x = 1;
    bool y = true;
in
    y = false;
