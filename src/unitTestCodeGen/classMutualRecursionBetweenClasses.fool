class paperino()
{
    int foo(pluto a)
        if a.gee() == 0 then { 1 } else { 20 };
}

class pluto(int b)
{
    int gee()
        b + 10;

    int zee()
        b - 100;
}

let
    paperino p = new paperino();
    pluto pl = new pluto(1);
in
    pl.gee() + pl.zee() + p.foo(pl);
