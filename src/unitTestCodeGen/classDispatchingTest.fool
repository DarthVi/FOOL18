class pippo(int a)
{
    int retVal()
        a + 1;

    bool gee()
        true;
}

class pluto extends pippo(int a)
{
    int retVal()
        a + 2;

    bool foo(bool b)
        b;
}

let
    pippo p = new pluto(4);
in
    p.retVal() ;