class pippo(int a) {
    int pluto(int c) 2;
    int minnie() a;
}

class pluto(bool barf)
{
    int abbaia()
        if barf then { 3 } else { 4 };
}

class paperino extends pippo(int a; int v) {
    int p() a+v;
    int sclera(pluto c) c.abbaia();
}

let
    paperino x = new paperino(3,5);
    pippo y = new pippo(1);
    pluto z = new pluto(true);

in
    x.p();
    //x.sclera(z);
    //x.pluto(5);
