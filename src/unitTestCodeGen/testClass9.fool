class ric() {

    int go(int k)
        let
            int x = k;
        in
            if(x==0) then {1}
                    else { x*this.go(x-1)};
}

let
    int n = 5;
    ric c = new ric ();
in
    c.go(5);