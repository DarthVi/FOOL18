class pippo(int a)
{
    int retVal()
        a + 1;

    bool gee()
        true;
}

class pluto extends pippo(int a; int b)
{
    int retVal()
        a + 2;

    bool foo(int c)
        if b > 0 then { true } else { false };
}

let
    int funArgClass(pippo x)
        x.retVal();
    pippo p = new pippo(4);

in
    funArgClass(p);

