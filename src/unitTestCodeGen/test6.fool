let
    int y = 5+2;
    int p = 12;
    bool z = true;

    bool faa(int n, int m)
    let
        int x = m;
    in
        if(n==m)
            then {true}
            else {
                if(m>=n)
                    then {faa(3,2)}
                    else { faa(1,1)}
            };

    bool caso()
    let
        bool pazzo = faa(1,10);
    in
        pazzo;

in
    caso();