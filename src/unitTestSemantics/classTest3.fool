class Pippo (bool postazione; int portatili)
{
    int f(int x, int y)
        x + y;
}

class Pluto extends Pippo (bool postazione; int portatili; int quaderno)
{
    void g(bool val)
        let
            bool x;
        in
            val = x;
}

class Paperino extends Pluto(bool postazione; int quaderno; int qui)
{
    bool h()
        let
            bool x = false;
        in
            if(x) then { true } else { x };
}

let
    int x = 1;
    bool y = true;
in
    y = false;
